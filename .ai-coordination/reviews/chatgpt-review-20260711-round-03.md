# ChatGPT 网页端审查报告

## 审查信息

- 审查时间：2026-07-11（Round 03 最终只读复核）
- 审查轮次：Round 03 最终复核
- 代码基线：`main / 5a7cd286077e8683478736a7738e41f70a6658f9 + 当前未提交修改 / 缓存 20260711-10`，基线时间 2026-07-11 20:37 CST
- 审查范围：当前基线、问题队列、正式测试报告和检查清单、缓存 `20260711-10` UI 审计、阶段四专项报告与脚本，以及 DATA-001、EXPORT-001、SEC-001、SEC-002 和剩余上线门槛。本轮仅审查已有代码与证据，没有重新执行浏览器、PDF、导出或真实设备测试。
- 本轮是否修改业务文件：否
- 本轮是否编辑或删除已有审查报告：否

## 总体结论

当前证据能够支持以下本地 Chromium 结论：缓存 `20260711-10` 的 UI 审计为 PASS，报告自带浏览器、Playwright bundled Chromium、Git 和缓存元数据；PDF 五类实际文件均进入预期成功或降级路径；备份的生词、阅读历史和语法数据完成实际导出、覆盖恢复与刷新持久化；SEC-002 的三类恶意协议被拒绝；PPTX、PNG、JPEG、CSV 和 Anki TSV 均触发真实下载，CSV/TSV 内容检查通过；localStorage 损坏、禁用和容量满场景下页面均未出现未捕获异常。未发现新的确定性代码 P0。

但现有证据尚不足以支持“本地上线门槛已全部完成”。第一，SEC-001 的阶段四动态用例没有打开恢复后的历史文章，也没有进入语法本渲染恶意语法字段，因此正式资料所写的 `text / annotatedHtml / grammar` 全部完成动态验证属于证据夸大；当前代码和静态断言显示修复方向正确，但按照项目对 P1 的回归证据规则，不建议在补齐精确动态路径前维持无保留的 VERIFIED。第二，当前 UI 审计只进入并翻转闪卡，没有完成一轮评分和结束状态，也没有验证新收藏生词刷新后仍存在；正式测试报告的这两项最终门槛仍为 PENDING，因此尚不能证明核心学习闭环在当前基线完整通过。第三，正式测试报告顶部已经切换至 `20260711-10`，正文、FLOW-AUTO、视觉证据和最终门槛仍多处引用 `20260711-09 / 12-16-39-382Z`，文档状态再次不一致。

因此本轮建议为：**暂缓上线**。这不是因为已经发现新的确定性产品 P0/P1，而是因为一个既有 P1 的精确动态回归仍未被现有脚本实际覆盖，且核心闪卡完成与生词刷新持久化仍缺少上线门槛证据。补齐这些本地测试并同步正式资料后，如无新失败，可转为“带条件上线”，继续把 Safari、iPhone、移动端真实下载、PPTX 在 PowerPoint/Keynote 中视觉打开和当前线上版本作为明确外部 PENDING；当前证据不支持“允许无条件上线”。

## 关键状态复核

| 项目 | 本轮复核结论 | 建议状态 |
|---|---|---|
| PDF 五类 | 文字型 PDF 提取并进入阅读；扫描、加密、损坏和超过 20 MB 文件均产生预期降级或拒绝，证据与正式报告一致。复杂排版仍属于 Beta 未覆盖边界。 | 对应五项 PASS 可以保留 |
| 备份覆盖恢复 | 实际 JSON 包含生词、历史和语法；清空后导入并刷新仍持久化。证据支持当前正式报告对该范围的 PASS。 | PASS 可以保留；重复项、损坏备份和全部数据类别仍不应扩写为已覆盖 |
| DATA-001 | 静态检查证明 `resetLevelTest()` 和页面内联逻辑不再绕过 `safeStorage`；但禁用存储动态用例只验证页面启动，没有实际点击重置或清除动作，损坏数据用例还写入了非实际生词键 `reading_vocab`，而应用使用 `reading_vocab_list`。 | 实现层面可维持 VERIFIED，但必须收窄“动态三类场景已覆盖全部相关动作”的表述，并补充定向动作测试 |
| EXPORT-001 | 远程分词关闭时导出直接使用页面现有标注的静态断言成立；五种文件真实下载，CSV/TSV 内容已检查。原 P1 调用链可以保持 VERIFIED。 | VERIFIED 可以保留；PowerPoint/Keynote 视觉打开和移动端真实下载保持 PENDING |
| SEC-001 | 当前代码从纯文本重新渲染历史文章且不再使用备份 `annotatedHtml`，单双引号转义静态断言成立；但现有动态脚本只渲染历史列表，没有打开历史文章或渲染语法本，不能支持“所有恶意字段动态通过”的表述。 | 建议暂回 FIXED，补齐精确动态回归后再恢复 VERIFIED |
| SEC-002 | 历史 URL 复用 HTTP(S) 白名单；当前 Chromium 用例确认 `javascript:`、`data:` 和空白混淆协议均被清空。 | VERIFIED 可以保留 |
| 未解决 P0/P1 | 未发现新的确定性代码 P0/P1；但 SEC-001 的 P1 回归证据不完整，核心闭环仍有上线门槛 PENDING。 | 不能勾选“未解决 P1 为零” |

## 问题清单

### TEST-006：核心闪卡完成和生词刷新持久化仍没有当前基线证据

- 建议等级：待确认（按上线阻断门槛处理）
- 证据层级：静态代码与自动化测试产物审查
- 涉及文件：`frontend/tools/ui-audit.mjs`、`frontend/audit-screenshots/2026-07-11T12-33-50-244Z/ui-audit-report.md`、`PRE_LAUNCH_TEST_REPORT.md`
- 发现：UI 审计的闪卡步骤只进入闪卡并点击卡面，没有选择“没记住 / 有点犹豫 / 记住了”、推进队列、验证结束状态或学习历史；审计也没有在收藏后刷新页面验证生词仍存在。正式报告第 364～365 行仍将“生词刷新后仍存在”和“闪卡可以完成一轮”保留为未完成的最终门槛。
- 证据位置：`frontend/tools/ui-audit.mjs` 的 `vocab page and flashcard` 步骤；`PRE_LAUNCH_TEST_REPORT.md` 第 134～143、354～375 行
- 可能影响：现有 PASS 只能证明闪卡入口和翻面可用，不能证明核心复习闭环可完成；若队列推进、持久化或完成状态存在故障，可能构成核心功能 P1。
- 建议：增加一个当前缓存定向流程：收藏生词 → 刷新 → 确认生词存在 → 进入闪卡 → 完成评分 → 验证完成状态、复习数据和历史记录；失败时按实际结果定级。
- 是否必须实际运行确认：是
- 是否可能阻止上线：是

### SEC-001：现有动态用例没有触发原漏洞的历史恢复路径

- 建议等级：P1（验证未闭环，不表示已确认修复失效）
- 证据层级：静态代码与自动化测试产物审查
- 涉及文件：`frontend/tools/stage4-audit.mjs`、`frontend/app.js`、`frontend/tools/check.mjs`、`PRE_LAUNCH_TEST_REPORT.md`、`.ai-coordination/ISSUE_QUEUE.md`
- 发现：阶段四脚本导入恶意备份后只执行 `switchWorkspace('history')` 和 `renderReadingHistory()`，检查 `#historyList` 中没有 `script` 或 `img`；它没有点击历史项“打开”或调用 `restoreHistoryArticle()`，也没有切换到语法页验证恶意 grammar 字段。由此只能动态确认历史列表中的 title/source 没有生成恶意节点，不能动态确认 text、annotatedHtml 和 grammar。当前 `restoreHistoryArticle()` 调用 `renderText()`、静态检查禁止写回 `annotatedHtml`，说明实现修复方向成立，但正式资料把未执行路径写成动态 PASS。
- 证据位置：`frontend/tools/stage4-audit.mjs` 第 96～109 行；`frontend/app.js` 第 7354～7363 行；`frontend/tools/check.mjs` 第 101～113 行；阶段四报告 SEC-001 条目
- 可能影响：P1 已有修复很可能正确，但当前证据不能满足项目规定的完整回归记录，也不能排除恶意正文在实际“打开历史文章”时经过其他渲染分支产生问题。
- 建议：导入恶意备份后依次打开历史文章、进入语法本并刷新页面，断言脚本标记始终未变化、页面中不存在恶意节点、恶意内容只以文本显示；测试通过后再把 SEC-001 恢复为 VERIFIED，并把正式报告的动态描述限定到实际步骤。
- 是否必须实际运行确认：是
- 是否可能阻止上线：是

### TEST-007：DATA-001 的动态证据没有覆盖原重置动作，损坏生词键名也不正确

- 建议等级：P2
- 证据层级：静态代码与自动化测试产物审查
- 涉及文件：`frontend/tools/stage4-audit.mjs`、`frontend/app.js`、`frontend/tools/check.mjs`、`.ai-coordination/ISSUE_QUEUE.md`
- 发现：禁用和容量满用例只打开页面并等待 `body` 可见，没有执行水平测试重置、数据清除、TTS 设置或保存动作。损坏数据用例写入 `reading_vocab`，而当前应用实际读取 `reading_vocab_list`；历史和语法损坏键仍被覆盖。静态搜索显示业务代码中的直接 `localStorage` 调用已收口到 `safeStorage` 内，因此代码修复可信，但问题队列所写“禁用、损坏和容量满三种动态场景均通过”容易被理解为相关用户动作均已验证。
- 证据位置：`frontend/tools/stage4-audit.mjs` 第 134～154 行；`frontend/app.js` 中 `reading_vocab_list` 的读取和保存；`frontend/tools/check.mjs` 第 90～96 行
- 可能影响：测试可能漏掉操作发生时的 UI 状态不一致、错误提示缺失或动作中断；当前不构成已知 P1，但会夸大 DATA-001 的动态覆盖。
- 建议：使用正确的 `reading_vocab_list` 损坏键；在禁用/容量满上下文中实际触发水平测试重置、设置保存和数据清除，断言不抛未捕获异常且 UI 反馈符合预期。正式资料在补测前应写成“页面启动降级通过＋实现静态检查通过”。
- 是否必须实际运行确认：是
- 是否可能阻止上线：否；若定向动作实际失败则重新定级

### DOC-001：正式测试报告再次混用缓存 20260711-09 与 20260711-10

- 建议等级：P2
- 证据层级：静态代码与文档审查
- 涉及文件：`PRE_LAUNCH_TEST_REPORT.md`、`PRE_LAUNCH_CHECKLIST.md`、`.ai-coordination/CURRENT_BASELINE.md`、`.ai-coordination/ISSUE_QUEUE.md`
- 发现：测试报告第 7～9 行正确写入缓存 `20260711-10`、最新 UI 报告 `12-33-50-244Z` 和阶段四报告；但第 13～15、106～107、344、360～361 行仍把当前流程和视觉证据写为 `20260711-09 / 12-16-39-382Z`。问题队列的 DOC-001 处理结论也只写同步到 `20260711-09`。检查清单虽然保留 Safari/iPhone，却没有明确列出移动端真实下载、PPTX PowerPoint/Keynote 视觉打开和当前线上版本三个外部 PENDING；测试报告只在最终结论段集中说明。
- 证据位置：`PRE_LAUNCH_TEST_REPORT.md` 第 7～18、94～108、330～375、389～400 行；`PRE_LAUNCH_CHECKLIST.md` 浏览器、功能完整性和高优先级章节；`CURRENT_BASELINE.md` 第 15～26 行
- 可能影响：读者无法判断问题状态和门槛勾选究竟对应缓存 09 还是 10，也可能把历史线上 PASS 误认为当前未提交版本已经上线验证；这会削弱最终上线结论的可追溯性。
- 建议：统一把当前自动化与视觉证据切换为 `20260711-10 / 12-33-50-244Z`，旧 `20260711-09` 仅作为历史证据；在检查清单和最终报告中增加独立“外部 PENDING”区，逐项列出 Safari、iPhone、移动端下载、PPTX 办公软件视觉打开和当前线上版本，并明确上线范围与回退条件。
- 是否必须实际运行确认：否
- 是否可能阻止上线：待确认；文档本身不是产品 P1，但正式资料未同步时不应作最终上线记录

## 最终上线建议

**建议：暂缓上线。**

当前不是“允许上线”：真实 Safari/iPhone、当前线上版本、移动端实际下载和 PPTX 办公软件视觉打开仍未完成。

当前也暂不满足“带条件上线”：除外部 PENDING 外，仍有本地核心闭环证据缺口（闪卡完整一轮、生词刷新持久化）和 SEC-001 精确 P1 回归缺口；同时正式资料尚未完全同步到缓存 `20260711-10`。完成这些本地项目、确认没有未解决 P0/P1、写明上线范围与回退条件后，可以转为带条件上线，不需要等待所有非核心 P2/P3 优化。

## 无法通过本轮审查确认

- 实际点击：本轮没有亲自运行项目；只复核现有脚本、报告和代码。闪卡完整一轮、生词刷新持久化、空输入、重复收藏、错误答案、重试和完整历史对应关系仍无法确认。
- PDF：已有阶段四证据支持五类指定文件；复杂排版、0 字节、不同生成器和真实 Safari/iPhone 中的 PDF 行为仍无法确认。
- 导出：已有证据支持本地 Chromium 下载和基础文件检查；移动端真实下载、PowerPoint/Keynote 视觉打开、跨软件字体与版式仍无法确认。
- TTS：真实日语音色、播放/停止、无音色降级和 Safari/iPhone 表现无法确认。
- 浏览器兼容：Mac Safari、iPhone Safari、微信内置浏览器、Firefox、触摸操作、键盘导航和 VoiceOver 无法确认；Chromium viewport 不替代真实设备。
- 线上环境：缓存 `20260711-10` 当前未提交版本是否已经部署、首次访问、缓存刷新、资源 404、控制台、线上 PDF/备份/导出和回退行为无法确认；早前线上 PASS 不自动覆盖当前版本。

## 建议 Codex 优先处理

1. 先补齐 SEC-001 的“导入恶意备份 → 打开历史文章 → 渲染语法本 → 刷新”动态回归，并完成“收藏 → 刷新 → 完整闪卡一轮 → 历史”核心闭环。
2. 补强 DATA-001 定向动作测试，修正损坏生词键名，并据真实步骤收窄或更新问题队列中的动态证据描述。
3. 将正式报告、检查清单和问题队列统一到缓存 `20260711-10`，明确外部 PENDING、上线范围和回退条件，再形成最终上线判断。

## 需要用户决定

无。
