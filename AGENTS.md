# CodexPro 使用规则

1. CodexPro 工具列表只查询一次。
2. 打开工作区后，直接使用 CodexPro3.read 读取文件。
3. 没有真正读取文件前，不得汇报进度。
4. 每次汇报进度时，必须写出已经读取的文件名。
5. 用户要求只读时，不得修改文件。
6. 每次完成任务时，都要展示当前任务的完成进度百分比。
7. 所有会改变用户可见页面的修改，必须读取并执行 `VISUAL_DELIVERY_WORKFLOW.md`：先完成自动测试，再生成桌面、窄桌面和手机截图；发现问题直接继续修改，只有内部截图检查通过后才能向用户展示候选。
