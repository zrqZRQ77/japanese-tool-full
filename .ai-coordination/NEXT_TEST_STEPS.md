# 下一轮上线前测试步骤

> 准备日期：2026-07-11
> 适用基线：当前未提交修改 / 缓存 `20260711-08`
> 原则：没有真实执行的项目保持 `PENDING` 或 `BLOCKED`，不沿用其他基线的 PASS。

## 1. Chromium 核心审计（已完成）

已在 `frontend` 目录使用 Playwright bundled Chromium 完成当前缓存审计：

- 报告：`frontend/audit-screenshots/2026-07-11T12-16-39-382Z/ui-audit-report.md`
- 结果：`PASS`
- Tokenizer mode：`built-in`
- 已补充：恶意历史 URL 协议拒绝、浏览器/基线元数据，以及 390/430/1280 的菜单、阅读导入、生词本导航与关键区域可见性
- 证据边界：仅覆盖报告列出的本地 Chromium 自动化流程，不替代以下专项测试或真实设备验收

## 2. PDF 实际文件

依次验证正常文字型、扫描图片型、加密、损坏和超过限制的 PDF。检查页面不会崩溃、文件输入可重置、错误提示可理解；正常 PDF 需确认正文确实进入阅读工作台。当前自动化只覆盖辅助逻辑，不能代替真实文件测试。

## 3. 备份恢复闭环

先创建生词、语法笔记、阅读历史和练习记录，导出备份；随后改变本地数据，再导入刚才的备份并刷新页面。逐项确认数据恢复、重复项处理和失败提示。另导入含恶意 `annotatedHtml` 的测试备份，确认脚本不执行且历史文章由纯文本重新生成。

## 4. 导出文件

实际下载 PPTX、PNG、JPEG、CSV 和 Anki TSV，核对文件能打开、格式和内容正确、日文与假名未乱码。远程智能分词关闭时，导出过程不得下载 Kuromoji 字典或导致页面卡死。

## 5. XSS 与异常输入

在正文、标题、语法笔记以及可恢复的备份字段中测试带单双引号的 HTML、`<script>` 和带事件属性的标签。确认内容只作为文本显示，控制台无脚本执行痕迹。

## 6. 最终真实设备

自动审计通过后，再执行 Mac Safari 和 iPhone Safari 的无痕首次访问、核心闭环、PDF、备份恢复和导出抽查。真实设备结果单独记录，不能由桌面尺寸模拟替代。
